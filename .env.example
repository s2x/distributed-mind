# Host path to the directory where brain.json is stored.
# This directory is mounted at /data in the web container; the app reads/writes /data/brain.json.
# Copy this file to .env and set your path (e.g. ./data or /home/user/mind-data).
BRAIN_DATA_DIR=./data
