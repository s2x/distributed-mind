#!/bin/bash

set -e

# Get the directory of this script
root_path=$(dirname "$0")

# Check if it's a completion request
if [ "$1" = "--complete" ]; then
    shift
    bun run ${root_path}/cli/src/complete.ts "$@"
    exit 0
fi

# Normal execution
bun run ${root_path}/cli/src/mind.ts "$@"
