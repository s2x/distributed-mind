services:
    web:
        build:
            context: .
            dockerfile: web/Dockerfile
        ports:
            - '3000:3000'
        environment:
            - MIND_DATA_DIR=/data
            - PORT=3000
        volumes:
            - ${BRAIN_DATA_DIR:-./data}:/data
        restart: unless-stopped
